# Project Checkpoint - 2026-06-02 20:35

## Project

- Root: `/Users/kyylian/shipproof`
- Git: `main`, clean before this checkpoint update, current HEAD `8b660be Update project checkpoint`
- Context: ShipProof `v0.2.0` product work is past report UX v1 and security baseline lifecycle; beta repo matrix work has started but is not complete.

## Done This Session

- Paused release/tag work because `v0.2.0` tag and GitHub release still need explicit user approval.
- Confirmed current release state:
  - package version is already `0.2.0`
  - only `v0.1.0` tag exists
  - GitHub release `v0.2.0` does not exist
  - package remains private and npm auth was previously missing
- Started the external beta test matrix using local real repos.
- Selected repos with existing dependencies and representative project shapes:
  - `/Users/kyylian/Developer/trustq`
  - `/Users/kyylian/Developer/net-cafe-system/admin-web`
  - `/Users/kyylian/Developer/cvboost`
  - `/Users/kyylian/Developer/unity-apple-scaffold-agent`
- Ran ShipProof smoke reports against 4 external repos.
- Parsed JSON and SARIF output for the completed beta runs.

## Current State

- ShipProof repo was clean before writing this checkpoint.
- Completed external reports: 4/5 target beta reports.
- All 4 completed beta runs passed with `decision: ship`.
- No beta candidate repo changes were reverted or cleaned up.
- Candidate repos were already dirty before testing; their user-owned changes must be left intact.
- Product work still needs:
  - one more safe external beta repo run, or an explicit decision to document the matrix at 4 runs
  - beta matrix documentation in ShipProof
  - optional changelog/package file-list updates if matrix docs should ship
  - final verification
  - PR/merge/checkpoint if we keep the same branch workflow

## Important Files / Artifacts

- `/private/tmp/shipproof-beta-trustq.json`: TrustQ beta JSON report.
- `/private/tmp/shipproof-beta-trustq.sarif`: TrustQ beta SARIF report.
- `/private/tmp/shipproof-beta-admin-web.json`: Admin web beta JSON report.
- `/private/tmp/shipproof-beta-admin-web.sarif`: Admin web beta SARIF report.
- `/private/tmp/shipproof-beta-cvboost.json`: Cvboost beta JSON report.
- `/private/tmp/shipproof-beta-cvboost.sarif`: Cvboost beta SARIF report.
- `/private/tmp/shipproof-beta-unity-apple-scaffold-agent.json`: Unity Apple scaffold agent beta JSON report.
- `/private/tmp/shipproof-beta-unity-apple-scaffold-agent.sarif`: Unity Apple scaffold agent beta SARIF report.
- `docs/checkpoints/LATEST.md`: will point to this pause checkpoint after update.

## Beta Results So Far

| Repo | Command Shape | Result |
| --- | --- | --- |
| `trustq` | `--changed src/server.js,package.json --no-browser` | passed, ship, score 88, checks `test`, `security-lite`, SARIF 0 results |
| `admin-web` | `--changed src/main.tsx,package.json --no-browser` | passed, ship, score 94, checks `lint`, `build`, `security-lite`, SARIF 0 results |
| `cvboost` | `--changed app/page.tsx,package.json --no-browser` | passed, ship, score 94, checks `lint`, `test`, `build`, `security-lite`, SARIF 0 results |
| `unity-apple-scaffold-agent` | `--changed src/cli.ts,package.json --no-browser` | passed, ship, score 94, checks `typecheck`, `test`, `build`, `security-lite`, SARIF 0 results |

## External Repo Status Notes

- `/Users/kyylian/Developer/trustq`: dirty before/after with checkpoint and evidence docs.
- `/Users/kyylian/Developer/net-cafe-system/admin-web`: parent repo dirty with `../AGENTS.md`, `../.gemini/`, `../.github/`.
- `/Users/kyylian/Developer/cvboost`: dirty with app, docs, package, test, and metadata changes.
- `/Users/kyylian/Developer/unity-apple-scaffold-agent`: dirty with untracked `.gemini/`, `.github/`, and `AGENTS.md`.
- Generated build output, if ignored by those repos, was left untouched.

## Verification

- ShipProof repo state check:
  - `git status --short`: clean before this checkpoint update
  - `git log --oneline -8`: latest HEAD `8b660be`
- Beta report parsing:
  - TrustQ JSON: `passed`, `ship`, score `88`; SARIF `2.1.0`, `0` results
  - Admin web JSON: `passed`, `ship`, score `94`; SARIF `2.1.0`, `0` results
  - Cvboost JSON: `passed`, `ship`, score `94`; SARIF `2.1.0`, `0` results
  - Unity Apple scaffold agent JSON: `passed`, `ship`, score `94`; SARIF `2.1.0`, `0` results
- Full ShipProof verification after beta matrix docs has not been run yet.

## Open Questions / Risks

- `v0.2.0` tag/release remains blocked on explicit approval.
- Npm publishing remains blocked by package privacy and missing npm auth.
- Need at least one more external/beta repo for the current 5-report release-readiness target.
- Some external beta repos are dirty; next work must avoid reverting or cleaning user-owned changes.
- Promptcraft and `tcli` are possible candidates but may write broader generated output or take longer.

## Next Steps

1. Inspect one additional beta candidate before running it, preferably a smaller npm project such as `trade-signal-bot`, `portfolio`, or `urun-yazari`.
2. Run ShipProof once more with JSON and SARIF output, then parse artifacts and check target repo status.
3. Create `docs/beta-test-matrix.md` summarizing all beta runs, gaps, commands, artifacts, and side effects.
4. Run full ShipProof verification:
   - `npm test`
   - `npm run smoke:github-mock`
   - `npm pack --dry-run`
   - `npm audit --omit=dev`
   - `git diff --check`
   - ShipProof self-proof with JSON and SARIF output
5. Only after explicit approval, create and push `v0.2.0` tag and GitHub release.

## Resume Prompt

Continue from this checkpoint. First read this file and `/Users/kyylian/AGENTS.md`, then inspect `git status --short --branch`, `git show-ref --tags`, `gh release view v0.2.0`, and the beta artifacts under `/private/tmp/shipproof-beta-*.json` and `/private/tmp/shipproof-beta-*.sarif`. Do not create the `v0.2.0` tag or release unless the user explicitly approves release actions.
