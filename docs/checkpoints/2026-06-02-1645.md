# Project Checkpoint - 2026-06-02 16:45

## Project

- Root: `/Users/kyylian/shipproof`
- Git: `main`, tracking `origin/main`
- Context: ShipProof is a public GitHub Action/CLI. Browser smoke v2 is merged and dogfooded through PR #2.

## Done This Session

- Implemented Browser smoke v2 controls:
  - dev server stdout/stderr logs under `browser.logDir`
  - `browser.readyUrl` readiness polling
  - shared `browser.timeoutMs` for readiness and route navigation
  - `browser.waitUntil` for Playwright navigation
  - advisory missing-Playwright behavior when `browser.required === false`
- Hardened dev server startup:
  - records log file paths in proof summaries
  - fails quickly when the dev server exits before readiness
  - reports log paths in readiness/early-exit failures
- Fixed config behavior found during self-review:
  - `browser.baseUrl` from config now reuses an existing server instead of starting `devCommand`
  - added regression coverage for this path
- Added Action/CLI inputs:
  - `browser-ready-url`
  - `browser-timeout-ms`
  - `browser-wait-until`
  - `browser-log-dir`
- Updated self-dogfood workflow:
  - passes `browser-log-dir`
  - uploads `shipproof-browser-logs` artifact with `if-no-files-found: ignore`
- Added docs:
  - `docs/browser-smoke.md`
  - updated `docs/configuration.md`
  - updated `README.md`
  - updated `CHANGELOG.md`
- Opened, verified, and merged dogfood PR:
  - PR: `https://github.com/kingkyylian/shipproof/pull/2`
  - Run: `https://github.com/kingkyylian/shipproof/actions/runs/26823552664`
  - Merge commit: `223e25c1a20004322f2268539e87609c594f30ac`
  - PR comment created by `github-actions` with ShipProof report marker

## Current State

- Current branch: `main`
- Product code head: `223e25c Merge pull request #2 from kingkyylian/browser-smoke-v2`
- Remote head: `origin/main` at `223e25c`
- `v0.1.0` still points to `51410c4 Finalize v0.1.0 release notes`
- Browser smoke v2 and agent feedback prompts are Unreleased after `v0.1.0`.
- `package.json` remains `"private": true`; npm publishing is still not enabled.

## Important Files / Artifacts

- `src/browser.js`: browser smoke planning, server readiness, log capture, Playwright route checks.
- `test/browser.test.js`: Browser v2 tests, including early exit, logs, readiness, advisory Playwright, config baseUrl reuse.
- `src/action.js`: GitHub Action browser input handling.
- `bin/shipproof.js`: local CLI browser flags and env fallbacks.
- `action.yml`: composite Action input surface.
- `.github/workflows/shipproof.yml`: self-dogfood workflow and artifact uploads.
- `docs/browser-smoke.md`: operator docs for browser smoke v2.
- `docs/superpowers/plans/2026-06-01-shipproof-production-roadmap.md`: broader roadmap.

## Verification

- Command: `node --test test/browser.test.js`
  - Result: passed, 15/15 tests
- Command: `npm test`
  - Result: passed, 54/54 tests
- Command: `npm run smoke:github-mock`
  - Result: passed
- Command: `npm pack --dry-run`
  - Result: passed, 17 files, 18.5 kB package
- Command: `node bin/shipproof.js --help`
  - Result: passed; new browser flags shown
- Command: `git diff --check`
  - Result: passed
- Command: `npm run shipproof -- --changed src/browser.js,test/browser.test.js,docs/browser-smoke.md,src/action.js,bin/shipproof.js,action.yml,.github/workflows/shipproof.yml --no-browser --json-report-path /private/tmp/shipproof-browser-v2-report.json`
  - Result: passed, `Decision: ship`, `Score: 100/100`
- GitHub dogfood:
  - PR #2 merged
  - Workflow `ShipProof / proof` passed
  - PR comment created with `Status: passed`, `Decision: ship`, `Score: 88/100`

## Open Questions / Risks

- Browser route inference is still basic; dynamic routes map to `/test` and complex app routers may need explicit routes.
- Browser logs are captured as files, but there is no structured log excerpt in the JSON report yet.
- Security-lite v2 is not started:
  - no line numbers
  - no allowlist
  - no SARIF
  - limited pattern context
- Monorepo support is not started:
  - no workspace/package-root grouping
  - no changed-package command targeting
- npm distribution remains disabled by `"private": true`.

## Next Steps

1. Implement Security-lite v2 with TDD:
   - line-numbered findings
   - allowlist config
   - optional SARIF/report artifact
   - better secret/auth/CORS context
2. Add monorepo support:
   - detect npm/pnpm workspaces
   - map changed files to package roots
   - run targeted package commands
3. Prepare next release path:
   - decide whether this is `v0.2.0`
   - update release notes
   - decide GitHub Action tag only vs npm package enablement

## Resume Prompt

Continue from this checkpoint. First read this file and `/Users/kyylian/AGENTS.md`, then inspect `src/security.js`, `test/security.test.js`, `src/core.js`, `src/config.js`, and `docs/superpowers/plans/2026-06-01-shipproof-production-roadmap.md`. Start with Security-lite v2 using TDD unless the user reprioritizes.
