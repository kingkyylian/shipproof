# Project Checkpoint - 2026-06-02 20:28

## Project

- Root: `/Users/kyylian/shipproof`
- Git: `main`, tracking `origin/main`
- Context: ShipProof is prepared for `v0.2.0`; report UX v1 and security baseline lifecycle are merged. Tag/release has not been created yet.

## Done This Session

- Continued after report UX v1 was merged.
- Implemented security baseline lifecycle on branch `security-baseline-v1`:
  - `security.baseline` config entries
  - baseline findings remain visible in Markdown and JSON
  - baseline findings do not fail `security-lite`
  - baseline findings do not reduce ship score
  - baseline findings are omitted from SARIF
  - expired baseline entries become active findings again
- Added TDD coverage:
  - config default/merge baseline support
  - scanner baseline matching
  - expired baseline behavior
  - SARIF baseline filtering
  - report rendering of baseline status/reason
- Opened security baseline PR:
  - PR: `https://github.com/kingkyylian/shipproof/pull/7`
  - Head: `a9e4a406a45452b776246323dd5a816c2bb65c97`
- Verified PR dogfood run:
  - Run: `https://github.com/kingkyylian/shipproof/actions/runs/26836601404`
  - Workflow: `ShipProof`
  - Conclusion: `success`
  - Job: `proof`
- Verified PR comment from `github-actions`:
  - `Status: passed`
  - `Decision: ship`
  - `Score: 94/100`
  - includes `Artifacts` section
- Downloaded and parsed PR artifacts:
  - Markdown report passed
  - JSON report parsed
  - SARIF report parsed
- Merged PR #7:
  - Merge commit: `b2c3bc95d0f545b7a73eaab367402d1646f4b72f`
  - Merged at: `2026-06-02T17:28:06Z`
- Pruned deleted remote branch:
  - `origin/security-baseline-v1`

## Current Product State

- Current branch: `main`
- Remote: `origin/main`
- Current commit: `b2c3bc9 Merge pull request #7 from kingkyylian/security-baseline-v1`
- `package.json` version: `0.2.0`
- `package-lock.json` version: `0.2.0`
- `package.json` still has `"private": true`; npm publishing is intentionally not enabled.
- Existing tags:
  - `v0.1.0`
- Missing release artifacts:
  - `v0.2.0` git tag does not exist yet.
  - GitHub release `v0.2.0` does not exist yet.
- Npm state from earlier check:
  - `npm view shipproof` returned `E404`, package name appears unpublished.
  - `npm whoami` returned `E401`, local npm auth is not configured.

## Important Files / Artifacts

- `src/security.js`: baseline status matching, active-finding counting, SARIF filtering.
- `src/config.js`: `security.baseline` defaults and merge behavior.
- `src/core.js`: baseline findings are visible in report status/reason and omitted from agent prompt active security list.
- `test/security.test.js`: baseline scanner/check/SARIF/score coverage.
- `test/config.test.js`: baseline config coverage.
- `test/core.test.js`: baseline report rendering coverage.
- `docs/security-lite.md`: baseline policy docs.
- `docs/configuration.md`: config example and policy docs.
- `docs/report-schema.md`: baseline finding fields.
- `/private/tmp/shipproof-pr7-artifacts.yNp4nE`: downloaded PR #7 artifacts for local inspection.

## Verification

- TDD targeted verification:
  - `node --test test/config.test.js test/security.test.js test/core.test.js`: initially failed on missing baseline config, baseline marking, SARIF filtering, and report status.
  - After implementation, `node --test test/config.test.js test/security.test.js test/core.test.js`: passed, 42/42 tests.
- Full local verification before PR:
  - `npm test`: passed, 84/84 tests
  - `npm run smoke:github-mock`: passed
  - `npm pack --dry-run`: passed, `shipproof@0.2.0`, 20 files, 26.1 kB package
  - `npm audit --omit=dev`: passed, 0 vulnerabilities
  - `git diff --check`: passed
  - `node bin/shipproof.js --help`: passed
  - ShipProof self-proof with JSON and SARIF output: passed, `Decision: ship`, `Score: 94`
  - JSON report parse: schemaVersion `1.0`, status `passed`, decision `ship`, score `94`, checkCount `2`, securityFindings `0`, artifacts present
  - SARIF parse: version `2.1.0`, runs `1`, results `0`
- GitHub dogfood:
  - PR #7 check rollup: `proof` completed with `SUCCESS`
  - Run #26836601404: completed with `success`
  - PR comment: `Status: passed`, `Decision: ship`, `Score: 94/100`, `Artifacts` section present
  - Markdown artifact: `Status: passed`, `Decision: ship`, `Score: 94/100`
  - JSON artifact: schemaVersion `1.0`, status `passed`, decision `ship`, score `94`, checkCount `2`, securityFindings `0`, artifacts object present
  - SARIF artifact: version `2.1.0`, runs `1`, results `0`

## Open Questions / Risks

- Tag/release has not been created because release/tag actions need explicit approval.
- Npm release is not ready:
  - package is private
  - local npm auth is missing
  - no npm trusted publishing workflow exists
- `v0.2.0` docs point to a tag that must be created before external users can install that exact reference.
- The broader product goal is not complete:
  - at least 5 external/beta repos still need successful reports for the roadmap's `v0.2.0` definition of done
  - beta repo test matrix is pending
  - more real-repo failure examples are needed for report UX tuning

## Next Steps

1. With explicit approval, create and push the GitHub Action tag:
   - `git tag v0.2.0 b2c3bc95d0f545b7a73eaab367402d1646f4b72f`
   - `git push origin v0.2.0`
2. With explicit approval, create a GitHub release:
   - use the `0.2.0` section from `CHANGELOG.md` as release notes
3. After tag/release, verify:
   - `git ls-remote --tags origin 'v0.2.0*'`
   - `gh release view v0.2.0 --json tagName,name,url,isDraft,isPrerelease,publishedAt`
   - dogfood a tiny PR or workflow using `kingkyylian/shipproof@v0.2.0`
4. Continue product roadmap:
   - beta repo test matrix
   - run ShipProof against real Next/Vite/monorepo repos
   - collect false positives and report UX issues

## Resume Prompt

Continue from this checkpoint. First read this file and `/Users/kyylian/AGENTS.md`, then inspect `git status --short --branch`, `git show-ref --tags`, `gh release view v0.2.0`, `package.json`, `CHANGELOG.md`, and the latest PR/run state. If the user explicitly approves release actions, create the `v0.2.0` tag and GitHub release, then verify the released action reference.
