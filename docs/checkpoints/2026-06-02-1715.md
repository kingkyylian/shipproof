# Project Checkpoint - 2026-06-02 17:15

## Project

- Root: `/Users/kyylian/shipproof`
- Git: `main`, tracking `origin/main`
- Context: ShipProof is a public GitHub Action/CLI. Browser smoke v2 and Security-lite v2 are merged and dogfooded.

## Done This Session

- Implemented Security-lite v2 controls:
  - line and column metadata when available
  - redacted source snippets
  - suppression guidance on findings
  - config-backed `security.allow`
  - allowlist expiry handling
  - required `id`, `file`, and `reason` for suppression
  - SARIF `2.1.0` generation
- Wired SARIF into:
  - local CLI via `--security-sarif-path`
  - GitHub Action input `security-sarif-path`
  - GitHub mode artifact writing
  - self-dogfood workflow artifact upload
- Updated reporting:
  - Markdown Security Findings table now shows `file:line` locations and snippets.
  - `docs/report-schema.md` documents line, column, snippet, and SARIF.
- Updated docs:
  - `docs/security-lite.md`
  - `docs/configuration.md`
  - `README.md`
  - `CHANGELOG.md`
- Updated package files list to include `docs/security-lite.md`.
- Updated mock GitHub smoke to verify SARIF artifact writing without leaving generated files in repo root.
- Opened, verified, and merged dogfood PR:
  - PR: `https://github.com/kingkyylian/shipproof/pull/3`
  - Run: `https://github.com/kingkyylian/shipproof/actions/runs/26824759100`
  - Merge commit: `9386dc16424cd1f6f022ac5929b63736ce629246`
  - PR comment created by `github-actions` with ShipProof report marker
  - SARIF artifact downloaded and parsed successfully

## Current State

- Current branch: `main`
- Product code head: `9386dc1 Merge pull request #3 from kingkyylian/security-lite-v2`
- Remote head: `origin/main` at `9386dc1`
- `v0.1.0` still points to `51410c4 Finalize v0.1.0 release notes`
- Unreleased after `v0.1.0`:
  - agent feedback prompts
  - Browser smoke v2
  - Security-lite v2
- `package.json` remains `"private": true`; npm publishing is still not enabled.

## Important Files / Artifacts

- `src/security.js`: Security-lite scanner, allowlist policy, redaction, SARIF generation.
- `test/security.test.js`: line/snippet/allowlist/SARIF regression coverage.
- `src/core.js`: security policy pass-through and Markdown location/snippet rendering.
- `src/action.js`: GitHub SARIF artifact write path.
- `bin/shipproof.js`: `--security-sarif-path` local and GitHub mode wiring.
- `action.yml`: `security-sarif-path` input.
- `.github/workflows/shipproof.yml`: SARIF artifact upload.
- `scripts/mock-github-smoke.mjs`: mock GitHub path verifies SARIF artifact output.
- `docs/security-lite.md`: Security-lite operator docs.
- `docs/superpowers/plans/2026-06-01-shipproof-production-roadmap.md`: broader roadmap.

## Verification

- Command: `npm test`
  - Result: passed, 59/59 tests
- Command: `npm run smoke:github-mock`
  - Result: passed; mock GitHub path verifies Markdown, JSON, SARIF, summary, and PR comment creation
- Command: `npm pack --dry-run`
  - Result: passed, 18 files, 21.2 kB package
- Command: `git diff --check`
  - Result: passed
- Command: `node bin/shipproof.js --help`
  - Result: passed; `--security-sarif-path` shown
- Command: `npm run shipproof -- --changed .github/workflows/shipproof.yml,CHANGELOG.md,README.md,action.yml,bin/shipproof.js,docs/configuration.md,docs/report-schema.md,docs/security-lite.md,package.json,scripts/mock-github-smoke.mjs,src/action.js,src/config.js,src/core.js,src/security.js,test/action.test.js,test/config.test.js,test/core.test.js,test/security.test.js --no-browser --json-report-path /private/tmp/shipproof-security-v2-full-report.json --security-sarif-path /private/tmp/shipproof-security-v2-full.sarif`
  - Result: passed, `Decision: ship`, `Score: 88/100`
- Command: parse `/private/tmp/shipproof-security-v2-full.sarif`
  - Result: SARIF `2.1.0`, 1 run, 0 results
- GitHub dogfood:
  - PR #3 merged
  - Workflow `ShipProof / proof` passed
  - PR comment created with `Status: passed`, `Decision: ship`, `Score: 88/100`
  - `shipproof-security-sarif` artifact downloaded and parsed: SARIF `2.1.0`, 1 run, 0 results
- Command: `npm audit --omit=dev`
  - Result: not run successfully; failed with `ENOLOCK` because the repo has no lockfile
  - Context: `package.json` currently has 0 dependencies and 0 devDependencies

## Open Questions / Risks

- Security-lite is still intentionally shallow:
  - no baseline file
  - no severity override config
  - no SARIF upload to GitHub code scanning API
  - limited framework-specific checks beyond secrets, CORS, env files, and auth-sensitive paths
- Monorepo support is not started:
  - no workspace/package-root grouping
  - no changed-package command targeting
  - no package-local browser Playwright loading
- Release path is not updated:
  - `v0.1.0` does not include Browser smoke v2 or Security-lite v2
  - npm package remains private
- Audit cannot run until a lockfile exists.

## Next Steps

1. Implement monorepo support with TDD:
   - detect npm/pnpm workspaces
   - map changed files to package roots
   - run targeted package commands
   - support package-local browser smoke dependencies
2. Prepare `v0.2.0` release path:
   - update release notes
   - decide GitHub Action tag only vs npm package enablement
   - decide whether to add a lockfile for audit compatibility
3. Improve report UX:
   - check failure snippets
   - SARIF artifact reference in Markdown
   - clearer remediation commands

## Resume Prompt

Continue from this checkpoint. First read this file and `/Users/kyylian/AGENTS.md`, then inspect `src/core.js`, `src/browser.js`, `src/security.js`, `package.json`, and `docs/superpowers/plans/2026-06-01-shipproof-production-roadmap.md`. Start with monorepo support using TDD unless the user reprioritizes.
