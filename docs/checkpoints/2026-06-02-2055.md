# Project Checkpoint - 2026-06-02 20:55

## Project

- Root: `/Users/kyylian/shipproof`
- Git: `main`, tracking `origin/main`, uncommitted release-readiness changes present
- Context: `v0.2.0` release-readiness work now includes six successful external reports, one browser advisory report, one caught failure example, and multiple beta-discovered correctness fixes.

## Done This Session

- Continued from the 20:45 checkpoint.
- Reconfirmed release state:
  - only tag present is `v0.1.0`
  - GitHub release `v0.2.0` still does not exist
  - no tag, release, commit, branch switch, or push was performed
- Added browser-smoke related fixes:
  - root browser dev commands now use detected package manager, for example `pnpm dev`
  - GitHub mode passes detected root package manager into root browser smoke planning
  - local CLI mode passes detected root package manager into root browser smoke planning
  - local `--screenshot-dir` now overrides default config in browser artifact reports
- Added CLI regression coverage in `test/cli.test.js`.
- Ran external browser advisory beta on `admin-web`:
  - Vite dev server started and became ready
  - missing Playwright became `browser-smoke:not_checked` because `browser.required` was `false`
  - report stayed `passed`, `ship`, score `94`
  - browser log artifact was written under `/private/tmp`
  - no dev server remained listening on port `4173`
- Ran large pnpm monorepo beta on `tcli`:
  - workspace-local commands used `pnpm --filter tcli`
  - typecheck, test, build, and security-lite all passed
  - report was `passed`, `ship`, score `100`
- Updated docs:
  - `docs/beta-test-matrix.md`
  - `docs/browser-smoke.md`
  - `docs/monorepo.md`
  - `README.md`
  - `CHANGELOG.md`

## Current State

- Working tree remains uncommitted.
- Untracked files include:
  - `docs/beta-test-matrix.md`
  - `docs/checkpoints/2026-06-02-2035.md`
  - `docs/checkpoints/2026-06-02-2045.md`
  - `docs/checkpoints/2026-06-02-2055.md`
  - `test/cli.test.js`
- `docs/checkpoints/LATEST.md` points to this checkpoint.
- Release/tag/publish actions remain pending explicit approval.

## Important Files / Artifacts

- `bin/shipproof.js`: local browser plan now preserves `--screenshot-dir` and root package manager.
- `src/action.js`: GitHub browser plan now preserves root package manager.
- `src/browser.js`: root dev command uses `formatRootCommand`.
- `src/core.js`: executed failed checks fail proof; root checks use detected package manager.
- `src/security.js`: failed optional checks reduce score.
- `test/action.test.js`: root browser package manager coverage.
- `test/browser.test.js`: root browser dev command coverage.
- `test/cli.test.js`: local browser artifact path override coverage.
- `test/core.test.js`: optional failure and root pnpm coverage.
- `docs/beta-test-matrix.md`: current beta evidence.

## Beta Evidence

Successful external reports:

- `trustq`: passed, ship, score 88; SARIF `2.1.0`, 0 results.
- `admin-web`: passed, ship, score 94; SARIF `2.1.0`, 0 results.
- `cvboost`: passed, ship, score 94; SARIF `2.1.0`, 0 results.
- `unity-apple-scaffold-agent`: passed, ship, score 94; SARIF `2.1.0`, 0 results.
- `handoffkit`: passed, ship, score 94; SARIF `2.1.0`, 0 results.
- `tcli-monorepo`: passed, ship, score 100; SARIF `2.1.0`, 0 results.

Browser advisory report:

- `admin-web-browser-advisory`: passed, ship, score 94; checks `lint`, `build`, `security-lite`, `browser-smoke:not_checked`; SARIF `2.1.0`, 0 results.
- Browser logs: `/private/tmp/shipproof-browser-beta-admin-web-logs/server.stdout.log`
- Screenshot artifact path in JSON: `/private/tmp/shipproof-browser-beta-admin-web-screens`

Failure example:

- `portfolio`: failed, no-ship, score 79; `lint` failed; SARIF `2.1.0`, 0 results.

## Verification

- RED tests were observed before browser fixes:
  - root browser dev command produced `npm run dev` instead of `pnpm dev`
  - GitHub root browser plan used `npm` instead of detected `pnpm`
  - local `--screenshot-dir` artifact stayed `shipproof-screenshots`
- Targeted verification:
  - `node --test test/browser.test.js test/action.test.js test/core.test.js test/workspace.test.js`: passed, 61/61
  - `node --test test/cli.test.js test/browser.test.js test/action.test.js`: passed, 27/27
- Full local verification:
  - `npm test`: passed, 89/89 tests
  - `npm run smoke:github-mock`: passed
  - `npm pack --dry-run`: passed, `shipproof@0.2.0`, 21 files, 28.2 kB; includes `docs/beta-test-matrix.md`
  - `npm audit --omit=dev`: passed, 0 vulnerabilities
  - `node bin/shipproof.js --help`: passed
  - `git diff --check`: passed
- ShipProof self-proof:
  - JSON: `/private/tmp/shipproof-self-beta-matrix.json`
  - SARIF: `/private/tmp/shipproof-self-beta-matrix.sarif`
  - JSON parsed: schemaVersion `1.0`, status `passed`, decision `ship`, score `88`, checks `test` and `security-lite`, securityFindings `0`
  - SARIF parsed: version `2.1.0`, results `0`

## Open Questions / Risks

- `v0.2.0` tag/release remains blocked on explicit approval.
- Npm release remains blocked by package privacy and npm auth/publishing policy.
- Full Playwright-enabled browser render matrix is still pending because no local Next/Vite beta candidate in this pass had `playwright` or `@playwright/test` installed.
- External beta repos with dirty worktrees were left untouched; do not revert their user-owned changes.

## Next Steps

1. Review the current diff.
2. If approved, commit this beta matrix/fix batch or create a PR branch following the existing ShipProof dogfood flow.
3. After explicit release approval, create and push `v0.2.0`, create the GitHub release, and verify `kingkyylian/shipproof@v0.2.0`.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `git diff --stat`, `docs/beta-test-matrix.md`, `bin/shipproof.js`, `src/action.js`, `src/browser.js`, `src/core.js`, `src/security.js`, and the new tests. Do not create commits, branches, tags, pushes, or releases unless the user explicitly approves that git/release action.
